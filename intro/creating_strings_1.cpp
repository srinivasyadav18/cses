#include <bits/stdc++.h>

#define ull unsigned long long

using namespace std;

unordered_set<string> ans;

void recurse(string &s,string build,int start){
	if (start ==(int) s.size() - 1) {
		ans.insert(build);
		return;
	}
	for (int i = start
}
int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);

	string input;
	cin >> input;

	
	return 0;
}

