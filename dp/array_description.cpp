#include <bits/stdc++.h>

#define ull unsigned long long
#define ll long long

const int mod_int = 1e9 + 7;
const ll mod_ll = 1e9 + 7;

using namespace std;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);

	int n,m;
	cin >> n >> m;

	vector<int> x(n);
	for (int &i : x) cin >> i;

	ll product = 1;

	if (x[0] == 
	for (int i = 1 ; i < n - 1 ; +i){
		if (x[i] == 0) {
			
		}
	}
	return 0;
}

